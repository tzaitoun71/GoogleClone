# Build artifacts of the pipeline, not source. Both are regenerated by running
# the crawler and indexer, and both can get large.
crawled/
index.json

# Python
__pycache__/
.venv/

# Frontend
node_modules/